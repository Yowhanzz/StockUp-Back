# Enable mod_rewrite
RewriteEngine On

# Set the base directory for your project
RewriteBase /OOP/public/

# Route all requests to src/main.php if they are not files or directories
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
#RewriteRule ^ ../src/main.php [QSA,L]
